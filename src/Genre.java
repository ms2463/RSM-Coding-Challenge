/**
 * Enum representing different genres for media items (e.g. films, TV shows).
 */
public enum Genre {
    Action,
    Comedy,
    Drama,
    Horror,
    Romance,
    SCI_FI,
    Thriller,
    War
}
